[supervisord]
nodaemon=true
logfile=/dev/stdout
logfile_maxbytes=0
pidfile=/var/run/supervisord.pid
childlogdir=/var/log/supervisor
loglevel=info
user=root

[unix_http_server]
file=/var/run/supervisor.sock
chmod=0700
username=dummy
password=dummy

[supervisorctl]
serverurl=unix:///var/run/supervisor.sock
username=dummy
password=dummy

[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

# Main Application Process
[program:app]
command=/bin/bash start.sh
directory=/app
autostart=true
autorestart=true
redirect_stderr=true
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0
priority=10
startsecs=5
stopwaitsecs=10
exitcodes=0

# OpenTelemetry Agent (Optional)
# Uncomment this section to enable OpenTelemetry instrumentation
# You'll need to set OTEL_ENABLED=true in your App Platform environment variables
# [program:otel-agent]
# command=bash -c 'if [ "$OTEL_ENABLED" = "true" ]; then opentelemetry-instrument --traces_exporter console --metrics_exporter console python -c "import time; time.sleep(99999)"; else sleep infinity; fi'
# directory=/app
# autostart=true
# autorestart=true
# stderr_logfile=/var/log/app/otel.err.log
# stdout_logfile=/var/log/app/otel.out.log
# priority=5
# startsecs=3
# stopwaitsecs=5

# Add more processes here as needed
# Example: Background worker, cron job, etc.
# [program:worker]
# command=python worker.py
# directory=/app
# autostart=true
# autorestart=true
# stderr_logfile=/var/log/app/worker.err.log
# stdout_logfile=/var/log/app/worker.out.log
# priority=15
